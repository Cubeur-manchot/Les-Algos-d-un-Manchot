"use strict";

const loadData = () => {
	// load page with fetch()
};

const buildAlgPage = algName => {

};

const createHtmlTag = (type, options) => {
	return {...document.createElement(type), options};
};
